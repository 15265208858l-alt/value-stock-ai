# A股价值研投｜商业化安全基线

## 当前阶段

项目已经冻结核心研究引擎，商业化限制放在独立的 `membership.py` 与 `commercial_guard.py` 层，不参与财务、估值、评分与投资决策计算。

## 当前免费体验规则

- 免费版：1 个不同股票代码的完整研究试用。
- 同一只已试用股票：允许重复查看，不重复消耗名额。
- 免费版：不开放同行多股票扩展研究。
- 专业会员：后续解除上述限制，并提供股票池、估值提醒、深度历史、专业报告和多股票比较。

## 安全边界

当前 `commercial_guard.py` 使用 Streamlit `session_state` 做产品原型阶段的会话级限制。这可以用于测试产品流程，但**不是最终商业级鉴权方案**。

正式收费前必须升级为服务端控制：

1. 服务端用户身份与登录会话。
2. 服务端会员等级与有效期。
3. 数据库记录试用资格、已研究股票和订单状态。
4. 支付平台回调后由服务端授予会员权益。
5. 每次研究请求在服务端再次校验权限，不能信任前端参数。
6. 关键接口增加频率限制、防刷和异常访问监控。
7. 不把支付密钥、数据库密码或管理员权限放入公开 GitHub 仓库。

## 商业行为原则

- 不伪造数据。
- 不通过降低风控标准制造更多“可以买”的结论。
- 不把研究工具包装成保证收益或确定性荐股产品。
- 会员付费不改变核心研究规则。
- 缺失数据必须明确显示“暂无/数据不足”。
- 研究结果属于研究辅助信息，不构成投资建议。

## 上线门禁

在接入真实支付前，不得把当前会话级试用限制宣称为“防破解”或“商业级安全”。正式商业版本必须完成“账号 + 服务端权限 + 数据库 + 支付回调 + 风控”闭环后再收费。
